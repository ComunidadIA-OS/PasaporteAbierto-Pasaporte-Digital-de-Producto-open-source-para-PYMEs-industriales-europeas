# Handoff — 2026-05-23 19:58

## Estado actual

- **Ticket / fase:** F1 — Fundación. T1, T2, T3 completados con spec + code-quality review. T4 sin iniciar.
- **Rama git:** `f1-01-fundation`
- **Último TODO cerrado:** T3 (Docker Compose con backend, frontend, Langfuse y Ollama opcional) — completo, APPROVED por code quality. Commit `c826b8a`.
- **TODOs pendientes (de la lista de la sesión):**
  - [ ] T4 — Modelo SQLite (5 tablas + Alembic + seed)
  - [ ] T5 — Schema YAML de plugins + loader
  - [ ] T6 — Plugin `batteries.yaml` (UE 2023/1542)
  - [ ] T7 — Router LiteLLM con `MODEL_BACKEND`
  - [ ] T8 — Langfuse: cliente + decoradores
  - [ ] T9 — README de setup + verificación 30-min + cierre F1
- **Working tree:** No hay código a medias ni build/tests rotos.
  - `M docs/ARCHITECTURE.md` — cambio Next 15 → Next 16 hecho a mitad de sesión, sin commitear.
  - Untracked: `CLAUDE.md`, `docs/superpowers/` (plan de F1), `docs/tickets/`, `.claude/commands/handoff.md`, `.claude/settings.json`.
  - El usuario decide si commitear estos artefactos antes de retomar.

## Decisiones tomadas

- **Pivote Next.js 15 → 16 a media sesión.** Cascaada de ajustes obligatorios en T2: Biome 1.x → 2.x (claves `organizeImports` y `files.ignore` cambiaron de sitio, parser CSS de Biome 2 no entiende Tailwind 4 `@theme inline`); Tailwind 4 elimina `tailwind.config.ts` (config vía CSS); `postcss.config.mjs` en vez de `.js`. Documentado en commits `0723a0d` y `548e932`. CLAUDE.md, ARCHITECTURE.md, F1.md y plan actualizados a "Next.js 16".
- **`backend depends_on langfuse: service_started` en docker-compose**, no `service_healthy` como decía el plan original. Razón: la imagen `langfuse/langfuse:2` no expone healthcheck, así que `service_healthy` rompería `docker compose config`. Desviación pragmática, no anotada explícitamente en el commit `c826b8a`.
- **Postgres aparece sólo como dependencia interna de Langfuse self-hosted**, puerto no expuesto al host. La invariante "no Postgres para la app" se mantiene (el backend usa SQLite). Documentado en el cuerpo del commit `c826b8a`.
- **Placeholders en T3 para no romper el build del Dockerfile**: `backend/alembic.ini` (con comentario inerte), `backend/alembic/.gitkeep` y `plugins/.gitkeep`. T4 sobreescribe `alembic.ini` y puebla `alembic/`; T5/T6 pueblan `plugins/`.
- **Workflow `superpowers:subagent-driven-development`** para ejecutar el plan: cada T = implementer subagent + spec-compliance reviewer + code-quality reviewer, con loops de fix entre ellos. Fixes pequeños van como commit propio (`fix(...)`, `chore(...)`), no como amend.

## Findings de calidad pendientes (no bloquean F1, pendientes de T9)

- **Important** (T3 review): `backend/Dockerfile` no hace `RUN mkdir -p /app/data`. El named volume `backend_data:/app/data` crea el dir en runtime, pero si alguien hace `docker run` aislado SQLite falla con "unable to open database file". Diferido a T9.
- **Minor** (T2 review): `frontend/app/layout.tsx` mantiene metadata boilerplate (`title: "Create Next App"`, `description: "Generated by create next app"`).
- **Minor** (T2 review): `frontend/.gitignore` no incluye `.turbo`.
- **Minor** (T3 review): commit `c826b8a` tiene asunto de 77 caracteres (límite convención: 72).

## Próximos pasos

1. **Decidir qué hacer con los artefactos de planning** antes de seguir. Opciones: (a) commit `docs(planning): añade plan de F1, CLAUDE.md y tickets` con todo lo untracked + `docs/ARCHITECTURE.md`; (b) dejarlos como están — son válidos a medias. Si se commitean, seguir la convención de `CLAUDE.md ## Convención de commits` (Conventional Commits, cuerpo explicando motivo, sin `Co-Authored-By: Claude`).
2. **Arrancar T4 — Modelo SQLite.** Texto íntegro en `docs/superpowers/plans/2026-05-23-fase-1-fundacion.md` sección "Task 4 — Modelo de datos SQLite (5 tablas + Alembic + seed)". Archivos a crear: `backend/src/app/db/session.py`, `backend/src/app/models/{sessions,documents,extracted_fields,audit_log,published_dpps}.py`, `backend/alembic/env.py`, `backend/alembic/versions/0001_initial.py`, `backend/scripts/{seed,reset_db}.py`, `backend/tests/test_models.py`. T4 sobreescribe `backend/alembic.ini` y reemplaza `backend/alembic/.gitkeep`. TDD: tests de schema + reversibilidad upgrade/downgrade primero.
3. **Continuar T5 → T6 → T7 → T8 → T9 en orden.** T5–T8 son independientes entre sí; sólo dependen de T1–T3 + T4. Cada T sigue el patrón `superpowers:subagent-driven-development`. T9 cierra F1 con README ≤30 min y barrido de los findings minor pendientes.
4. **Antes de cerrar F1**: aplicar el fix `RUN mkdir -p /app/data` en `backend/Dockerfile` (finding Important de T3 review) y la limpieza minor de T2 (metadata de `layout.tsx`, `.turbo` en gitignore).
