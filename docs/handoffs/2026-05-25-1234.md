# Handoff — 2026-05-25 12:34

## Estado actual

- **Ticket / fase:** Consulta de análisis — sin implementación en esta sesión
- **Rama git:** f2_01_04
- **Último TODO cerrado:** ninguno (sesión sin lista activa — fue sesión de análisis/Q&A)
- **TODOs pendientes (de la lista de la sesión):**
  - (ninguno abierto)
- **Working tree:** `docs/superpowers/plans/2026-05-25-f2-01-04-corpus-ingesta-tests.md` modificado (no tocado en esta sesión — venía de antes).

## Decisiones tomadas

Ninguna decisión no obvia en esta sesión.

## Contexto de la sesión

La sesión fue exclusivamente análisis. Se respondieron dos preguntas:

1. **Dependencias F3 → F2:** F3-01 (Clasificador) y F3-04 (Chat lateral) dependen de F2-03 (servicio `retrieve`). F3-02 (Recolector PDFs) y F3-03 (Verificador) son independientes de F2.

2. **Qué puede adelantar la usuaria mientras trabaja en F2-01/F2-04** y su compañera en F2-02/F2-03. Mapa completo de tickets sin dependencia de F2:
   - F1-01 a F1-05 (fundación — todos independientes)
   - F3-02 Recolector PDFs (IA sin RAG)
   - F3-03 Verificador (determinista)
   - F5-01 a F5-04 (generación/publicación — todo independiente)
   - F4-01, F4-03, F4-04 (wizard, pasos sin IA)
   - F6-03, F6-04, F6-05 (docs/comunidad)

   **Recomendación dada:** si F1 está cerrado, priorizar F3-02 (Recolector) y F5-04 (firma + audit chain). Si F1 aún no está cerrado, F1-03 (plugin YAML + batteries.yaml) es el cuello de botella que desbloquea más tickets simultáneamente.

## Próximos pasos

1. **Verificar estado de F1:** revisar qué hay implementado en la rama (buscar `pyproject.toml`, `docker-compose.yml`, `plugins/_schema.yaml`) para saber qué prerequisitos están listos.
2. **Si F1-03 no está hecho:** implementar `plugins/_schema.yaml` y `plugins/batteries.yaml` — desbloquea F3-03, F4-03, F5-01, F5-02.
3. **Si F1 está completo:** arrancar F3-02 Recolector de PDFs — endpoint `POST /sessions/{id}/extract`, pipeline `pdfplumber` + LLM, `asyncio.gather` por PDF, estados `verified`/`self_declared`/`required_pending` en `extracted_fields`.
4. **Paralelamente:** F2-01 (ingesta corpus) continúa en la misma rama `f2_01_04` — revisar el plan en `docs/superpowers/plans/2026-05-25-f2-01-04-corpus-ingesta-tests.md`.
