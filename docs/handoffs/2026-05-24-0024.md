# Handoff — 2026-05-24 00:24

## Estado actual

- **Ticket / fase:** F1 — Fundación. T5 (F1-03 parte 1) cerrado y commiteado. T6 (F1-03 parte 2 — `batteries.yaml` UE 2023/1542) pausado a la espera de decisión del usuario sobre alcance.
- **Rama git:** `f1-03-plugins`
- **Último TODO cerrado:** T5 · Commit final (controller) — completo, commit `bdf4555 feat(plugins): schema YAML + loader con validación (F1-03)` en local, no pusheado.
- **TODOs pendientes (de la lista de la sesión):**
  - [ ] T6 · Implementer — Plugin batteries.yaml UE 2023/1542 (sin commit) — bloqueado en decisión de alcance
  - [ ] T6 · Spec compliance review
  - [ ] T6 · Code quality review
  - [ ] T6 · Commit final (controller)
  - [ ] T7 — Router LiteLLM con `MODEL_BACKEND`
  - [ ] T8 — Langfuse: cliente + decoradores
  - [ ] T9 — README de setup + verificación 30-min + cierre F1
- **Working tree:**
  - Dos archivos untracked, ninguno parte de un commit:
    - `backend/tests/test_batteries_plugin.py` (29 líneas) — el subagent del T6 alcanzó a escribir el archivo de tests antes de que se interrumpiera la dispatch. No hay aún `plugins/batteries.yaml`, así que estos tests fallarán con "FileNotFoundError" si se ejecutan. **Decidir antes de retomar T6: mantener este archivo o borrarlo y regenerar desde el subagent nuevo.**
    - `docs/research/battery-pass-v1.3-mandatory-attrs.md` (175 líneas) — extracto persistido del Battery Pass Consortium v1.3 con los 73 atributos mandatorios per BattReg, mapeo de citas erróneas del plan y las 4 opciones de alcance abiertas para T6.
  - Sin builds rotos ni tests rojos: la suite vive en el commit `bdf4555` con 13/13 verde, ruff limpio. El archivo de tests untracked NO se ejecuta a menos que se renombre o se cree el YAML.

## Decisiones tomadas

- **Política de commit-tras-validación aplicada por primera vez en T5** (cierre F1-02 → F1-03). El implementer subagent ya **no commitea**: deja working tree con cambios untracked y el controller (yo) commitea solo después de que ambos reviewers (spec compliance + code quality) hayan aprobado. Persistido en memoria como `feedback-commit-after-validation.md`. Aplicado en T5 con éxito tras un ciclo de `CHANGES_REQUESTED` → fix → `APPROVED`.

- **`PluginValidationError` como única superficie de fallo del loader.** Tanto `yaml.YAMLError` (YAML mal formado) como `pydantic.ValidationError` (schema inválido) se capturan y se reenvuelven con `from e`. Decisión surgió del code review de T5: para que T6+ no tenga que importar pyyaml/pydantic solo para capturar fallos de parseo de plugins. Aplica a §"Capa de datos y conocimiento" de ARCHITECTURE.md.

- **`extra="forbid"` en los 5 modelos Pydantic del loader.** Sin esto, un typo de un contribuidor (p. ej. `citaton:` en vez de `citation:`) se silencia y luego aparece como "campo faltante" en lugar de "clave desconocida". El gatekeeper tiene que ser estricto en la frontera. Aplica a invariante de extensibilidad por configuración de CLAUDE.md.

- **Validación de T6 contra Battery Pass Consortium v1.3 reveló que el plan original es insuficiente.** Los 17 campos propuestos cubren ~22% del mínimo regulatorio per Reg. 2023/1542 según la longlist industrial v1.3 (marzo 2026, 73 atributos mandatorios para EV/Industrial>2kWh). Además 5+ campos del plan tienen citas erróneas (citan Anexo VI Parte A cuando la cita canónica para contenidos del DPP es Anexo XIII). Detalle completo en `docs/research/battery-pass-v1.3-mandatory-attrs.md`. **EUR-Lex truncó la respuesta al consultar el reglamento vía WebFetch** — no devolvió artículos ni anexos, solo considerandos; por eso la validación se hizo contra la referencia industrial Battery Pass, no contra el texto regulatorio directo.

- **Decisión de alcance de T6 diferida al usuario.** 4 opciones abiertas (A: Tier-1 ampliado ~30 campos, mi recomendación; B: mantener 17 + corregir citas + marcar v0.1.0; C: cobertura completa 73 campos; D: diferir T6 hasta F2). Documentadas literalmente en `docs/research/battery-pass-v1.3-mandatory-attrs.md` §"Decisión pendiente para T6".

- **Setup de integración vía PRs en GitHub establecido como patrón.** La sesión hizo 3 PRs (T1-T3 `f1-01-fundation`, T4 `f1-02-models`, F6-06 `docs/f6-06-expediente`), todos mergeados ya en `origin/main` por el usuario. T5 (`f1-03-plugins`) sigue el mismo patrón cuando se decida pushear. Razón: el proyecto aspira a Digital Public Good (F6-05), y main probablemente está protegida → los PRs son la vía limpia.

- **Tres anotaciones pendientes para F6, registradas como F6-06 candidato en `docs/tickets/F6.md`** (commit `afc4bf4`, ya mergeado): vista "expediente técnico" imprimible defendible ante inspección MSA. Está marcado explícitamente como candidato — no compromete F6.

## Próximos pasos

1. **Decidir alcance de T6** entre las 4 opciones documentadas en `docs/research/battery-pass-v1.3-mandatory-attrs.md` §"Decisión pendiente para T6". Mi recomendación: **opción A (Tier-1 ampliado ~30 campos)** porque es honesto + defendible + se ejecuta en 1-2h sin romper el ritmo de F1.

2. **Decidir qué hacer con `backend/tests/test_batteries_plugin.py` untracked.** Tres opciones: (a) borrarlo y dejar que el subagent de T6 lo regenere desde cero según la opción A/B/C elegida; (b) mantenerlo si la opción elegida es B (los 4 tests del plan original siguen siendo válidos para 17 campos); (c) si se elige opción A o C, el archivo necesita expandirse para cubrir las aserciones adicionales.

3. **Decidir si commitear `docs/research/battery-pass-v1.3-mandatory-attrs.md` en una rama separada (`docs/research-battery-pass-v1.3`) o dejarlo viajar en `f1-03-plugins` con T6.** Lógicamente es research que justificó una decisión de scope, así que tiene valor commitear aunque no se elija ninguna opción aún. Sugerencia: PR docs aparte, igual que se hizo con F6-06 en `docs/f6-06-expediente`.

4. **Actualizar el plan F1 antes de implementar T6** según CLAUDE.md ("Si una decisión técnica o funcional contradice estos documentos, el documento gana: actualiza el doc antes de implementar"). Concretamente, editar `docs/superpowers/plans/2026-05-23-fase-1-fundacion.md` líneas 1393-1554 (sección T6) con la opción elegida + las citas corregidas. Solo cuando esté actualizado, dispatch del implementer subagent siguiendo el patrón establecido en T5 (sin commit en el implementer; commit por el controller tras dos APPROVED).

5. **Pushear `f1-03-plugins` a origin cuando se decida.** Comando: `git push -u origin f1-03-plugins`. Después abrir PR `f1-03-plugins → main` desde GitHub web UI. La rama hoy contiene 1 commit (`bdf4555`); cuando T6 cierre tendrá 2 commits y se mergea como F1-03 completa.

6. **Tras cerrar F1-03 (T5+T6), continuar con T7 → T8 → T9 en orden.** T7-T8 son independientes; T9 cierra F1 con README + barrido de findings Minor acumulados de T2, T3, T4, T5 (lista completa en este handoff y en `docs/handoffs/2026-05-23-1958.md`).
